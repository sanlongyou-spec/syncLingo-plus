<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- 业务日志级别可用环境变量覆盖：排查问题时把 SI_LOG_LEVEL 设为 DEBUG
       获取最细粒度日志，排查完成后改回 INFO（或删除该变量）。 -->
  <property name="SI_LOG_LEVEL" value="${SI_LOG_LEVEL:-INFO}"/>

  <!-- 所有日志写到 System.err，Docker 容器里 stderr 是行缓冲，立即可见 -->
  <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
    <target>System.err</target>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- 文件 appender：按天滚动，单文件上限 200MB，保留 7 天 -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>/app/logs/app.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>/app/logs/app-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <maxFileSize>200MB</maxFileSize>
      <maxHistory>7</maxHistory>
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
      <charset>UTF-8</charset>
    </encoder>
  </appender>

  <root level="WARN">
    <appender-ref ref="STDERR"/>
    <appender-ref ref="FILE"/>
  </root>

  <logger name="com.si.backend" level="${SI_LOG_LEVEL}" additivity="false">
    <appender-ref ref="STDERR"/>
    <appender-ref ref="FILE"/>
  </logger>

  <logger name="com.microsoft.cognitiveservices.speech" level="INFO" additivity="false">
    <appender-ref ref="STDERR"/>
    <appender-ref ref="FILE"/>
  </logger>

  <!-- integration 跟随 SI_LOG_LEVEL；设为 DEBUG 时可看到语音性别请求体、
       Cartesia chunk/flush 等最细粒度日志。 -->
  <logger name="com.si.backend.integration" level="${SI_LOG_LEVEL}" additivity="false">
    <appender-ref ref="STDERR"/>
    <appender-ref ref="FILE"/>
  </logger>
</configuration>
