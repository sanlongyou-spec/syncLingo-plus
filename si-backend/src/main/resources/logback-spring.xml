<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- 所有日志写到 System.err，Docker 容器里 stderr 是行缓冲，立即可见 -->
  <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
    <target>System.err</target>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- 文件 appender：按天滚动，单文件上限 200MB，保留 7 天 -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>/app/logs/app.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>/app/logs/app-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <maxFileSize>200MB</maxFileSize>
      <maxHistory>7</maxHistory>
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
      <charset>UTF-8</charset>
    </encoder>
  </appender>

  <root level="WARN">
    <appender-ref ref="STDERR"/>
    <appender-ref ref="FILE"/>
  </root>

  <logger name="com.si.backend" level="INFO" additivity="false">
    <appender-ref ref="STDERR"/>
    <appender-ref ref="FILE"/>
  </logger>

  <logger name="com.microsoft.cognitiveservices.speech" level="INFO" additivity="false">
    <appender-ref ref="STDERR"/>
    <appender-ref ref="FILE"/>
  </logger>

  <!-- integration 默认 INFO；Cartesia chunk/flush 等高频 DEBUG 已关闭 -->
  <!-- 排查特定问题时可临时把某个类改回 DEBUG，例如 AzureAsrIntegration -->
  <logger name="com.si.backend.integration" level="INFO" additivity="false">
    <appender-ref ref="STDERR"/>
    <appender-ref ref="FILE"/>
  </logger>
</configuration>
